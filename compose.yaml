services:
  app:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DATABASE_PATH: /data/mahjong-score.db
    volumes:
      - mahjong-data:/data
    restart: unless-stopped

volumes:
  mahjong-data:
